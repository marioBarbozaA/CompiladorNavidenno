 /_ esto es un comentario inicial _/

function float func1(char x22,char x22){
	local char x22 <= '!'| 
	local char miChar<='!'|
	local char miChar2<='!'| 
	local string str1<="Hola $%&/#$&) mundo"|
	local float fl1|
	local float fl1<=56.6*true|
	local int in1<= false|
	local char camilo[lit] <= {'a','b'}| 
	camilo[lit] <= 'c'|
	local bool mibool <= true|

	local int in1<= --fl1 ~ -14/++in1+7- 15 **  camilo[67]/camilo[lit] * func1 (hola, true, "hola mundo", 4.5, 'a') | 
	mibool <= (3.7 * fl1 + (-45.6~76)) < 56 ^ true # mibool # !mibool  | 

	return -5.6|

} 

function float miFunc(int dif,char otra,char otra2){	
	for(i <= 10|i<30*2|++i)
	{
		do{
			var <= var - 1| 
			return 1|
		}until(var2>12.2 # (34+33)>12)| 

		if (var===0)
		{
			print("entra al if")|
			break|
		}
		elif(true)
		{
			print(var)|
		}
		else
		{
			for(j <= 10|j<30*2|++j){
				for(k <= 10|k<30*2|++k){
					return 1+3|
				}|
			}|
		}|
		if (var===0){
			break|
		} elif(true) {
			print(var)|
		}|
	}|
	return 1| 
}

function string func3(string b1){
	return b1| 
}

function  main(){ 

	

	local bool b1| @marioneta
	local string s1| 
	read(s1)| 
	print(b1)| 
	print(true)| 
	print(-6.7)|
	miFunc(miFunc(),'a')| 
	local bool bl0 <= 6.7 !== 8.9| 
	local bool bl1 <= in1 => fl1 ^ false # !(func2(3,in1) > 56)|
	return bl1| 
}


